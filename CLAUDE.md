# Jade collaboration

Read COORDINATION.md and run the shared status check before changing this project:
`powershell -NoProfile -File .coordination/coordinate.ps1 -Action status`.
This is the same coordination protocol used by Codex. An already-running desktop
conversation must explicitly reload it; this file does not synchronize conversations.
